name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # 10분마다 실행 (UTC 기준)
  workflow_dispatch:         # 필요할 때 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl to Render app
        run: curl -s https://bandana-kmdl.onrender.com > /dev/null